npm install json-server --save-dev

console.log("REAL METHOD: " + method  + "---> " + req.url)
    console.log("Headers: " )
    console.log("Content-type : " + req.get('Content-type'))
    console.log("abc : " + req.get('abc'))  
    console.log("Body: " )
    console.log(req.body)
    console.log("=======================================")

node server.js --host x.x.x.x
curl -d "@data.json" -XPOST x.x.x.x
